name: zbg_proximity
description: BLE proximity detection engine for FlutterFlow research apps.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.0
  crypto: ^3.0.3
  collection: ^1.18.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  # No assets or fonts needed for this package.
